#pragma once

namespace GLCore {
	class Renderer {
	public:
		Renderer() {}
		~Renderer() {}

		void Clear();
		//Draw(const IndexBuffer& indexBuffer, const VertexArray& vertexArray, const Shader& shader) const
	};
}